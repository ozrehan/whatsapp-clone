"use strict";
window.W = window.W || {};

/* =====================================================================
   bot.js — RETIRED.
   Replies, typing simulation and tick progression now come from the
   server API (see js/api.js + polling in js/app.js). This file is kept
   as a no-op stub so the <script> tag in index.html keeps working.
   ===================================================================== */

W.bot = {
  sendText: function () {},
  sendContent: function () {},
  progressTicks: function () {}
};
